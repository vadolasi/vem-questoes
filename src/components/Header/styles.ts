import { styled } from "@/styles";

export const Container = styled('header', {
    width: '100%',
    padding: '23px 42px',

    display: "flex",
    alignItems: 'center',
    justifyContent: 'space-between',
    
})